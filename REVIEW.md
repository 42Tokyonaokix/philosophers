# プロジェクト簡易レビュー

以下はリポジトリ (philo) のコードレビュー要約です。深刻度は「重大/注意/改善推奨」で表記します。

## 重大（優先修正）
- `philo/srcs/app/validation.c`: `ft_calloc(sizeof(int), 6)` の引数順序が誤っており、確保サイズが期待と異なります。`ft_calloc(6, sizeof(int))` に修正してください。
- `philo/srcs/app/setup.c`: 右フォークの割当でオフバイワンがあります。ループ内の条件 `if (i != system->num_philos)` は誤りで、最後の哲学者が `forks[i+1]` を参照して範囲外アクセスする恐れがあります。`i != system->num_philos - 1` のように修正してください。
- `philo/srcs/utils/pthread.c`: スレッド生成失敗時の早期 return によるメモリリークや、`free_threads` の join ループで `pthread_t` をポインタとして扱っている点（不適切）。作成済みスレッド数で join を制御し、失敗時にも確実に配列を free してください。
- `philo/srcs/utils/gettime.c`: 時間を `int` で扱っておりオーバーフローの恐れがあります。`long`/`long long` へ変更し、`get_ms()`/`get_time()` の型を統一してください。また `wait_until_time()` のループでの比較/更新順を修正してください。

## 同期・競合に関する注意点
- `philo/srcs/utils/mutex.c` と `setup` の確保/解放で `n + 1` の扱いが一貫していません。確保サイズと解放ループの上限を合わせてください。
- `print` / `dead` mutex を用いた状態管理は概ね良好ですが、エラー伝搬や pthread の戻り値 (0 成功) を明示的に扱うと堅牢性が増します。

## 小規模改善（推奨）
- 時間管理関数を `long` 戻り値にして型を統一する。
- `create_threads()` が monitor を最初に作成する設計のままにするなら、失敗処理と作成順のコメントを追加すると可読性が上がります。
- `wait_*` の busy-wait 回数や `USLEEP` 定数は実行環境に合わせてチューニング可能です。

## 次のステップ（推奨順）
1. `validation.c` の calloc 修正とビルド確認
2. `setup.c` の右フォーク参照の修正と簡単な実行テスト
3. `pthread.c` の join/cleanup ロジック修正（作成済みスレッド数で join）
4. `gettime.c` の型修正と wait ループの見直し

必要ならこれらの修正を私がパッチで適用してコミット・push します。push が失敗する場合は認証情報かリモート設定の確認が必要です。

----
Generated by code review automation (assistant).
